/** @generated by @calimero/abi-codegen — DO NOT EDIT.
 *  Codegen fixes applied: Rust list types → TS arrays, map return → Record. */

import { MeroJs } from '@calimero-network/mero-react';

// ---- Generated types ----

export interface Expense {
  id: string;
  payer: string;
  amount: number;
  description: string;
  split_among: string[];
  created_at: number;
}

export interface Payment {
  id: string;
  from: string;
  to: string;
  amount: number;
  created_at: number;
}

export interface TripMetadata {
  id: string;
  name: string;
  currency: string;
  created_at: number;
}

export interface Event_TripCreated {
  id: string;
  name: string;
}

export interface Event_ExpenseAdded {
  id: string;
  payer: string;
}

export interface Event_ExpenseEdited {
  id: string;
}

export interface Event_ExpenseDeleted {
  id: string;
}

export interface Event_PaymentRecorded {
  id: string;
  from: string;
  to: string;
}

export type AbiEvent =
  | { name: 'TripCreated'; payload: Event_TripCreated }
  | { name: 'ExpenseAdded'; payload: Event_ExpenseAdded }
  | { name: 'ExpenseEdited'; payload: Event_ExpenseEdited }
  | { name: 'ExpenseDeleted'; payload: Event_ExpenseDeleted }
  | { name: 'PaymentRecorded'; payload: Event_PaymentRecorded };

// ---- Client class ----

export class TripClient {
  private _mero: MeroJs;
  private _contextId: string;
  private _executorPublicKey: string;

  constructor(mero: MeroJs, contextId: string, executorPublicKey: string) {
    this._mero = mero;
    this._contextId = contextId;
    this._executorPublicKey = executorPublicKey;
  }

  /** init — called automatically on context creation; no params. */
  public async init(): Promise<void> {
    await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'init',
      argsJson: {},
      executorPublicKey: this._executorPublicKey,
    });
  }

  /** create_trip — name and configure the trip. Must be called by the creator. */
  public async createTrip(params: { name: string; currency: string }): Promise<string> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'create_trip',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as string;
  }

  /** add_expense — log an expense paid by the caller, split among participants. */
  public async addExpense(params: {
    amount: number;
    description: string;
    split_among: string[];
  }): Promise<string> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'add_expense',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as string;
  }

  /** list_expenses — all expenses sorted chronologically. */
  public async listExpenses(): Promise<Expense[]> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'list_expenses',
      argsJson: {},
      executorPublicKey: this._executorPublicKey,
    });
    return response as Expense[];
  }

  /** edit_expense — update amount/description. Only the original author may do this. */
  public async editExpense(params: {
    id: string;
    new_amount: number;
    new_description: string;
  }): Promise<void> {
    await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'edit_expense',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
  }

  /** delete_expense — remove an expense. Only the original author may do this. */
  public async deleteExpense(params: { id: string }): Promise<void> {
    await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'delete_expense',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
  }

  /** get_balances — net balances keyed by participant identity. Positive = owed to them. */
  public async getBalances(): Promise<Record<string, number>> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'get_balances',
      argsJson: {},
      executorPublicKey: this._executorPublicKey,
    });
    return response as Record<string, number>;
  }

  /** record_payment — log a settlement payment from one participant to another. */
  public async recordPayment(params: {
    from: string;
    to: string;
    amount: number;
  }): Promise<string> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'record_payment',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as string;
  }
}
