/** @generated by @calimero/abi-codegen — fixed for team-todos ABI. */

import { MeroJs } from '@calimero-network/mero-react';

// ── Generated types ──────────────────────────────────────────────────────────

export interface Task {
  id: string;
  author: string;
  description: string;
  done: boolean;
  created_at: number;
}

export interface Event_TaskAdded {
  id: string;
}

export interface Event_TaskToggled {
  id: string;
}

export interface Event_TaskEdited {
  id: string;
}

export interface Event_TaskDeleted {
  id: string;
}

export type AbiEvent =
  | { name: 'TaskAdded'; payload: Event_TaskAdded }
  | { name: 'TaskToggled'; payload: Event_TaskToggled }
  | { name: 'TaskEdited'; payload: Event_TaskEdited }
  | { name: 'TaskDeleted'; payload: Event_TaskDeleted };

// ── Client class ─────────────────────────────────────────────────────────────

export class TodosClient {
  private _mero: MeroJs;
  private _contextId: string;
  private _executorPublicKey: string;

  constructor(mero: MeroJs, contextId: string, executorPublicKey: string) {
    this._mero = mero;
    this._contextId = contextId;
    this._executorPublicKey = executorPublicKey;
  }

  /**
   * add_task — mutate
   * Adds a new task to the shared list. Returns the generated task id.
   */
  public async addTask(params: { description: string }): Promise<string> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'add_task',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as string;
  }

  /**
   * toggle_task — mutate
   * Flips the done/undone status of an existing task.
   */
  public async toggleTask(params: { task_id: string }): Promise<void> {
    await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'toggle_task',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
  }

  /**
   * edit_task — mutate
   * Updates the description of a task the caller authored.
   */
  public async editTask(params: { task_id: string; new_description: string }): Promise<void> {
    await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'edit_task',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
  }

  /**
   * delete_task — mutate
   * Removes a task the caller authored.
   */
  public async deleteTask(params: { task_id: string }): Promise<void> {
    await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'delete_task',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
  }

  /**
   * list_tasks — view
   * Returns all tasks in the shared list.
   */
  public async listTasks(): Promise<Task[]> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'list_tasks',
      argsJson: {},
      executorPublicKey: this._executorPublicKey,
    });
    return (response as Task[]) ?? [];
  }
}
