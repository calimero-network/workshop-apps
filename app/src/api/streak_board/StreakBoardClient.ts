/** @generated by @calimero/abi-codegen — fixed for codegen bugs. */

import { MeroJs } from '@calimero-network/mero-react';

// ── Generated types ──────────────────────────────────────────────────────────

export interface Habit {
  id: string;
  author: string;
  title: string;
  current_streak: number;
  longest_streak: number;
  last_check_in_date: string;
  created_at: number;
}

export interface CheckIn {
  id: string;
  author: string;
  habit_id: string;
  date: string;
  timestamp: number;
}

export interface Cheer {
  id: string;
  author: string;
  habit_id: string;
  message: string;
  created_at: number;
}

export interface Event_HabitCreated {
  id: string;
  title: string;
  author: string;
}

export interface Event_CheckedIn {
  habit_id: string;
  date: string;
  author: string;
}

export interface Event_CheerSent {
  habit_id: string;
  author: string;
  message: string;
}

export type AbiEvent =
  | { name: 'HabitCreated'; payload: Event_HabitCreated }
  | { name: 'CheckedIn'; payload: Event_CheckedIn }
  | { name: 'CheerSent'; payload: Event_CheerSent };

// ── Client ───────────────────────────────────────────────────────────────────

export class StreakBoardClient {
  private _mero: MeroJs;
  private _contextId: string;
  private _executorPublicKey: string;

  constructor(mero: MeroJs, contextId: string, executorPublicKey: string) {
    this._mero = mero;
    this._contextId = contextId;
    this._executorPublicKey = executorPublicKey;
  }

  /** create_habit(title: String) → app::Result<String> */
  public async createHabit(params: { title: string }): Promise<string> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'create_habit',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as string;
  }

  /** list_habits() → app::Result<Vec<Habit>> */
  public async listHabits(): Promise<Habit[]> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'list_habits',
      argsJson: {},
      executorPublicKey: this._executorPublicKey,
    });
    return response as Habit[];
  }

  /** check_in(habit_id: String, date: String) → app::Result<String> */
  public async checkIn(params: { habit_id: string; date: string }): Promise<string> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'check_in',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as string;
  }

  /** get_check_ins(habit_id: String) → app::Result<Vec<CheckIn>> */
  public async getCheckIns(params: { habit_id: string }): Promise<CheckIn[]> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'get_check_ins',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as CheckIn[];
  }

  /** send_cheer(habit_id: String, message: String) → app::Result<String> */
  public async sendCheer(params: { habit_id: string; message: string }): Promise<string> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'send_cheer',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as string;
  }

  /** get_cheers(habit_id: String) → app::Result<Vec<Cheer>> */
  public async getCheers(params: { habit_id: string }): Promise<Cheer[]> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'get_cheers',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as Cheer[];
  }

  /** get_leaderboard() → app::Result<Vec<Habit>> */
  public async getLeaderboard(): Promise<Habit[]> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'get_leaderboard',
      argsJson: {},
      executorPublicKey: this._executorPublicKey,
    });
    return response as Habit[];
  }
}
