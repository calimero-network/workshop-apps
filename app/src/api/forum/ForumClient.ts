/** @generated by @calimero/abi-codegen — manually adapted for trading-pod-forum. */

import { MeroJs } from '@calimero-network/mero-react';

// ── Entity types ─────────────────────────────────────────────────────────────

export interface Post {
  id: string;
  author: string;
  title: string;
  body: string;
  post_type: string;   // "thesis" | "post-mortem" | "general"
  created_at: number;
}

export interface Reply {
  id: string;
  post_id: string;
  author: string;
  body: string;
  created_at: number;
}

// ── ABI events ────────────────────────────────────────────────────────────────

export type AbiEvent =
  | { name: 'PostCreated'; payload: { id: string } }
  | { name: 'PostEdited';  payload: { id: string } }
  | { name: 'PostDeleted'; payload: { id: string } }
  | { name: 'ReplyCreated'; payload: { id: string } }
  | { name: 'ReplyEdited';  payload: { id: string } }
  | { name: 'ReplyDeleted'; payload: { id: string } }
;

// ── Client ────────────────────────────────────────────────────────────────────

export class ForumClient {
  private _mero: MeroJs;
  private _contextId: string;
  private _executorPublicKey: string;

  constructor(mero: MeroJs, contextId: string, executorPublicKey: string) {
    this._mero = mero;
    this._contextId = contextId;
    this._executorPublicKey = executorPublicKey;
  }

  /** Create a new post. Returns the generated post id. */
  public async createPost(params: {
    title: string;
    body: string;
    post_type: string;
  }): Promise<string> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'create_post',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as string;
  }

  /** Edit the body of an existing post (author-only on the backend). */
  public async editPost(params: { id: string; new_body: string }): Promise<void> {
    await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'edit_post',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
  }

  /** Delete a post (author-only on the backend). */
  public async deletePost(params: { id: string }): Promise<void> {
    await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'delete_post',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
  }

  /** List all posts in the forum (chronological, oldest first). */
  public async listPosts(): Promise<Post[]> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'list_posts',
      argsJson: {},
      executorPublicKey: this._executorPublicKey,
    });
    return response as Post[];
  }

  /** Reply to a post. Returns the generated reply id. */
  public async replyToPost(params: { post_id: string; body: string }): Promise<string> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'reply_to_post',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as string;
  }

  /** Edit the body of an existing reply (author-only on the backend). */
  public async editReply(params: { id: string; new_body: string }): Promise<void> {
    await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'edit_reply',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
  }

  /** Delete a reply (author-only on the backend). */
  public async deleteReply(params: { id: string }): Promise<void> {
    await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'delete_reply',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
  }

  /** Fetch all replies for a given post. */
  public async getReplies(params: { post_id: string }): Promise<Reply[]> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'get_replies',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as Reply[];
  }
}
