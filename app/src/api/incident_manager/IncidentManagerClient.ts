/** @generated by @calimero/abi-codegen — patched for TS (Rust-type fixes applied). */

import { MeroJs } from '@calimero-network/mero-react';

// ── Domain types ──────────────────────────────────────────────────────────────

export interface Incident {
  id: string;
  title: string;
  description: string;
  severity: string;          // "P1" | "P2" | "P3" | "P4"
  status: string;            // "open" | "acknowledged" | "resolved"
  reported_by: string;
  commander: string | null;
  root_cause: string | null;
  created_at: number;        // u64 epoch ms
  resolved_at: number | null;
}

export interface TimelineEntry {
  id: string;
  incident_id: string;
  author: string;
  body: string;
  entry_type: string;        // "status_update" | "escalation" | "note" | etc.
  created_at: number;
}

export interface Postmortem {
  id: string;
  incident_id: string;
  summary: string;
  root_cause: string;
  action_items: string;
  last_edited_by: string;
  created_at: number;
}

export interface OnCallSlot {
  id: string;
  member_name: string;
  member_id: string;
  start_time: number;
  end_time: number;
  order: number;             // u32
}

// ── Events ───────────────────────────────────────────────────────────────────

export type AbiEvent =
  | { name: 'IncidentReported' }
  | { name: 'IncidentAcknowledged' }
  | { name: 'IncidentEscalated' }
  | { name: 'IncidentResolved' }
  | { name: 'TimelineEntryPosted' }
  | { name: 'PostmortemCreated' }
  | { name: 'PostmortemUpdated' }
  | { name: 'OnCallSlotSet' }
  | { name: 'OnCallSlotRemoved' };

// ── Client ───────────────────────────────────────────────────────────────────

export class IncidentManagerClient {
  private _mero: MeroJs;
  private _contextId: string;
  private _executorPublicKey: string;

  constructor(mero: MeroJs, contextId: string, executorPublicKey: string) {
    this._mero = mero;
    this._contextId = contextId;
    this._executorPublicKey = executorPublicKey;
  }

  // ── Incident mutations ───────────────────────────────────────────────────

  /** Report a new incident. Returns the new incident id. */
  public async reportIncident(params: {
    title: string;
    description: string;
    severity: string;
  }): Promise<string> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'report_incident',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as string;
  }

  /** Acknowledge an open incident; caller becomes the incident commander. */
  public async acknowledgeIncident(params: { incident_id: string }): Promise<void> {
    await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'acknowledge_incident',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
  }

  /** Escalate an open incident to the next responder. Returns the new timeline entry id. */
  public async escalateIncident(params: {
    incident_id: string;
    next_responder_id: string;
  }): Promise<string> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'escalate_incident',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as string;
  }

  /** Resolve an incident with a root-cause tag. */
  public async resolveIncident(params: {
    incident_id: string;
    root_cause: string;
  }): Promise<void> {
    await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'resolve_incident',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
  }

  /** Post a timeline entry. Returns the new entry id. */
  public async postTimelineEntry(params: {
    incident_id: string;
    body: string;
    entry_type: string;
  }): Promise<string> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'post_timeline_entry',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as string;
  }

  // ── Incident views ───────────────────────────────────────────────────────

  /** List incidents, optionally filtered by status. */
  public async listIncidents(params: { status_filter: string | null }): Promise<Incident[]> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'list_incidents',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as Incident[];
  }

  /** Fetch a single incident by id. */
  public async getIncident(params: { incident_id: string }): Promise<Incident> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'get_incident',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as Incident;
  }

  /** Fetch all timeline entries for an incident, ordered chronologically. */
  public async getTimeline(params: { incident_id: string }): Promise<TimelineEntry[]> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'get_timeline',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as TimelineEntry[];
  }

  // ── Postmortem mutations ─────────────────────────────────────────────────

  /** Create a postmortem for a resolved incident. Returns the postmortem id. */
  public async createPostmortem(params: {
    incident_id: string;
    summary: string;
    root_cause: string;
    action_items: string;
  }): Promise<string> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'create_postmortem',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as string;
  }

  /** Update an existing postmortem. */
  public async updatePostmortem(params: {
    postmortem_id: string;
    summary: string;
    root_cause: string;
    action_items: string;
  }): Promise<void> {
    await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'update_postmortem',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
  }

  /** Fetch the postmortem for an incident (null if none exists yet). */
  public async getPostmortem(params: { incident_id: string }): Promise<Postmortem | null> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'get_postmortem',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as Postmortem | null;
  }

  // ── On-call schedule mutations ───────────────────────────────────────────

  /** Add or update an on-call slot. Returns the slot id. */
  public async setOncallSlot(params: {
    member_name: string;
    member_id: string;
    start_time: number;
    end_time: number;
    order: number;
  }): Promise<string> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'set_oncall_slot',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as string;
  }

  /** Remove an on-call slot. */
  public async removeOncallSlot(params: { slot_id: string }): Promise<void> {
    await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'remove_oncall_slot',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
  }

  // ── On-call schedule views ───────────────────────────────────────────────

  /** Fetch the full on-call schedule, ordered by slot.order. */
  public async getOncallSchedule(): Promise<OnCallSlot[]> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'get_oncall_schedule',
      argsJson: {},
      executorPublicKey: this._executorPublicKey,
    });
    return response as OnCallSlot[];
  }
}
