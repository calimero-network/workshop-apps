/** @generated by @calimero/abi-codegen — DO NOT EDIT (codegen fixes applied). */

import { MeroJs } from '@calimero-network/mero-react';

// Generated types

export interface Poll {
  id: string;
  title: string;
  status: string;
  created_at: number;
}

export interface PollOption {
  id: string;
  label: string;
  author: string;
  created_at: number;
}

export interface Ranking {
  id: string;
  author: string;
  ordered_option_ids: string;
  submitted_at: number;
}

export type AbiEvent =
  | { name: 'PollCreated' }
  | { name: 'OptionAdded' }
  | { name: 'OptionRemoved' }
  | { name: 'RankingSubmitted' }
  | { name: 'PollClosed' };

export class VotingClient {
  private _mero: MeroJs;
  private _contextId: string;
  private _executorPublicKey: string;

  constructor(mero: MeroJs, contextId: string, executorPublicKey: string) {
    this._mero = mero;
    this._contextId = contextId;
    this._executorPublicKey = executorPublicKey;
  }

  /** create_poll */
  public async createPoll(params: { title: string }): Promise<string> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'create_poll',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as string;
  }

  /** add_option */
  public async addOption(params: { label: string }): Promise<string> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'add_option',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as string;
  }

  /** remove_option */
  public async removeOption(params: { option_id: string }): Promise<void> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'remove_option',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as void;
  }

  /**
   * submit_ranking
   * ordered_option_ids: Vec<String> in Rust → string[] in TypeScript
   */
  public async submitRanking(params: { ordered_option_ids: string[] }): Promise<string> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'submit_ranking',
      argsJson: params,
      executorPublicKey: this._executorPublicKey,
    });
    return response as string;
  }

  /** get_poll */
  public async getPoll(): Promise<Poll> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'get_poll',
      argsJson: {},
      executorPublicKey: this._executorPublicKey,
    });
    return response as Poll;
  }

  /**
   * get_options
   * Returns Vec<PollOption> in Rust → PollOption[] in TypeScript
   */
  public async getOptions(): Promise<PollOption[]> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'get_options',
      argsJson: {},
      executorPublicKey: this._executorPublicKey,
    });
    return response as PollOption[];
  }

  /**
   * get_rankings
   * Returns Vec<Ranking> in Rust → Ranking[] in TypeScript
   */
  public async getRankings(): Promise<Ranking[]> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'get_rankings',
      argsJson: {},
      executorPublicKey: this._executorPublicKey,
    });
    return response as Ranking[];
  }

  /** close_poll */
  public async closePoll(): Promise<void> {
    const response = await this._mero.rpc.execute({
      contextId: this._contextId,
      method: 'close_poll',
      argsJson: {},
      executorPublicKey: this._executorPublicKey,
    });
    return response as void;
  }
}
