/** @generated by @calimero/abi-codegen — DO NOT EDIT. */

import {
  MeroJs,
} from '@calimero-network/mero-react';

// Generated types

export interface AppState {
  invitations: Record<string, string>;
  messages: Record<string, string>;
}

export interface Event_InvitationAccepted {
  id: string;
  acceptor_id: string;
}

export interface Event_InvitationCreated {
  id: string;
  code: string;
}

export interface Event_MessageSent {
  id: string;
  chat_id: string;
}

export interface Invitation {
  id: string;
  code: string;
  creator_id: string;
  accepted: boolean;
  acceptor_id: string;
  created_at: number;
}

export interface Message {
  id: string;
  chat_id: string;
  sender_id: string;
  text: string;
  timestamp: number;
}




export type AbiEvent =
  | { name: "InvitationCreated"; payload: Event_InvitationCreated }
  | { name: "InvitationAccepted"; payload: Event_InvitationAccepted }
  | { name: "MessageSent"; payload: Event_MessageSent }
;


/**
 * Utility class for handling byte conversions in Calimero
 */
export class CalimeroBytes {
  private data: Uint8Array;

  constructor(input: string | number[] | Uint8Array) {
    if (typeof input === "string") {
      // Hex string
      this.data = new Uint8Array(
        input.match(/.{1,2}/g)?.map((byte) => parseInt(byte, 16)) || []
      );
    } else if (Array.isArray(input)) {
      // Number array
      this.data = new Uint8Array(input);
    } else {
      // Uint8Array
      this.data = input;
    }
  }

  toArray(): number[] {
    return Array.from(this.data);
  }

  toUint8Array(): Uint8Array {
    return this.data;
  }

  static fromHex(hex: string): CalimeroBytes {
    return new CalimeroBytes(hex);
  }

  static fromArray(arr: number[]): CalimeroBytes {
    return new CalimeroBytes(arr);
  }

  static fromUint8Array(bytes: Uint8Array): CalimeroBytes {
    return new CalimeroBytes(bytes);
  }
}

/**
 * Convert CalimeroBytes instances to arrays for WASM compatibility
 */
function convertCalimeroBytesForWasm(obj: any): any {
  if (obj === null || obj === undefined) {
    return obj;
  }

  if (obj instanceof CalimeroBytes) {
    return obj.toArray();
  }

  if (Array.isArray(obj)) {
    return obj.map(item => convertCalimeroBytesForWasm(item));
  }

  if (typeof obj === "object") {
    const result: any = {};
    for (const [key, value] of Object.entries(obj)) {
      result[key] = convertCalimeroBytesForWasm(value);
    }
    return result;
  }

  return obj;
}

/**
 * Convert arrays back to CalimeroBytes instances from WASM responses
 */
function convertWasmResultToCalimeroBytes(obj: any): any {
  if (obj === null || obj === undefined) {
    return obj;
  }

  if (Array.isArray(obj) && obj.every(item => typeof item === "number")) {
    return new CalimeroBytes(obj);
  }

  if (Array.isArray(obj)) {
    return obj.map(item => convertWasmResultToCalimeroBytes(item));
  }

  if (typeof obj === "object") {
    const result: any = {};
    for (const [key, value] of Object.entries(obj)) {
      result[key] = convertWasmResultToCalimeroBytes(value);
    }
    return result;
  }

  return obj;
}

export class KvStoreClient {
  private mero: MeroJs;
  private contextId: string;
  private executorPublicKey: string;

  constructor(mero: MeroJs, contextId: string, executorPublicKey: string) {
    this.mero = mero;
    this.contextId = contextId;
    this.executorPublicKey = executorPublicKey;
  }

  /**
   * init
   */
  public async init(): Promise<void> {
    const response = await this.mero.rpc.execute({ contextId: this.contextId, method: 'init', argsJson: {}, executorPublicKey: this.executorPublicKey });
    return response as void;
  }

  /**
   * create_invitation
   */
  public async createInvitation(params: { creator_id: string }): Promise<string> {
    const response = await this.mero.rpc.execute({ contextId: this.contextId, method: 'create_invitation', argsJson: params, executorPublicKey: this.executorPublicKey });
    return response as string;
  }

  /**
   * accept_invitation
   */
  public async acceptInvitation(params: { code: string; acceptor_id: string }): Promise<string> {
    const response = await this.mero.rpc.execute({ contextId: this.contextId, method: 'accept_invitation', argsJson: params, executorPublicKey: this.executorPublicKey });
    return response as string;
  }

  /**
   * send_message
   */
  public async sendMessage(params: { chat_id: string; sender_id: string; text: string }): Promise<void> {
    const response = await this.mero.rpc.execute({ contextId: this.contextId, method: 'send_message', argsJson: params, executorPublicKey: this.executorPublicKey });
    return response as void;
  }

  /**
   * get_messages
   */
  public async getMessages(params: { chat_id: string }): Promise<Message[]> {
    const response = await this.mero.rpc.execute({ contextId: this.contextId, method: 'get_messages', argsJson: params, executorPublicKey: this.executorPublicKey });
    return response as Message[];
  }

  /**
   * get_invitation
   */
  public async getInvitation(params: { code: string }): Promise<Invitation> {
    const response = await this.mero.rpc.execute({ contextId: this.contextId, method: 'get_invitation', argsJson: params, executorPublicKey: this.executorPublicKey });
    return response as Invitation;
  }

}