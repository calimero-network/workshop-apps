// Legacy entry-point — superseded by BoardPage for the task-tracker app.
// Kept as a stub so the file reference doesn't cause compilation errors
// in case the router or other tooling references this path.
export { default } from '../board/BoardPage';
