// Generated by Calimero Studio (deterministic e2e floor). Safe to edit:
// the floor only writes this file if it does not already exist. The verifier-
// writer subagent enriches `test.skip` lines into real assertions; you can
// too. Do NOT delete the smoke test (it's the floor the verify gate trusts).
import { test, expect } from '@playwright/test';
import { loginViaHash, clearAuth } from './helpers';

test.describe(`player: roll the dice on my turn, move a token, and get an extra roll if I roll a six`, () => {
  test.beforeEach(async ({ page }) => {
    await loginViaHash(page, 0);
  });

  test.afterEach(async ({ page }) => {
    await clearAuth(page);
  });

  // Smoke: the post-auth screen renders without a visible error banner.
  // Every story spec carries this; together they prove the auth + bundle
  // install + initial render path works on N nodes.
  test('post-auth screen renders without error', async ({ page }) => {
    await expect(page).toHaveURL(/.+/);
    const errorBanner = page.locator('text=/error|failed/i').first();
    await expect(errorBanner).toBeHidden({ timeout: 5_000 }).catch(() => {});
  });

  // [Verifier] NOTE: BoardView (action-roll_dice / action-move_token) only
  // mounts once the room has started, which per topology requires exactly 4
  // joined players (minMembersToStart === maxMembers === 4). The e2e harness
  // authenticates at most 3 distinct identities (loginViaHash supports
  // indices 0..2 only — see the-match-to-stay-waiting-until-all-4-se.spec.ts,
  // which proves the match never leaves the waiting gate below 4 seats), so
  // a match can never reach Active/started state via the UI in this harness
  // and these two criteria have no reachable affordance to drive here.
  test.fixme(`only the player whose turn it is can roll or move; after they act, all 4 players see the updated dice value, token position, and next turn within 5s`, async ({ page }) => {
    await expect(page).toHaveURL(/.+/);
  });

  test.fixme(`when a player rolls a six, the turn does not pass and the same player is prompted to roll again`, async ({ page }) => {
    await expect(page).toHaveURL(/.+/);
  });
});
