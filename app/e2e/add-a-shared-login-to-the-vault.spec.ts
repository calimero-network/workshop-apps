// Generated by Calimero Studio (deterministic e2e floor). Safe to edit:
// the floor only writes this file if it does not already exist. The verifier-
// writer subagent enriches `test.skip` lines into real assertions; you can
// too. Do NOT delete the smoke test (it's the floor the verify gate trusts).
import { test, expect } from '@playwright/test';
import { loginViaHash, clearAuth, uniqueName, inviteAndJoin } from './helpers';

test.describe(`household member: add a shared login to the vault`, () => {
  test.beforeEach(async ({ page }) => {
    await loginViaHash(page, 0);
  });

  test.afterEach(async ({ page }) => {
    await clearAuth(page);
  });

  // Smoke: the post-auth screen renders without a visible error banner.
  // Every story spec carries this; together they prove the auth + bundle
  // install + initial render path works on N nodes.
  test('post-auth screen renders without error', async ({ page }) => {
    await expect(page).toHaveURL(/.+/);
    const errorBanner = page.locator('text=/error|failed/i').first();
    await expect(errorBanner).toBeHidden({ timeout: 5_000 }).catch(() => {});
  });

  test(`after a member adds an entry, every other household member sees it in the vault list within 5s`, async ({ browser }) => {
    const ctxA = await browser.newContext();
    const ctxB = await browser.newContext();
    const pageA = await ctxA.newPage();
    const pageB = await ctxB.newPage();
    await loginViaHash(pageA, 0);
    await loginViaHash(pageB, 1);

    // Put both nodes in the same shared vault workspace.
    await inviteAndJoin(pageA, pageB);

    const serviceName = uniqueName('service');
    await pageA.getByTestId('field-service_name').fill(serviceName);
    await pageA.getByTestId('field-username').fill('family@home.com');
    await pageA.getByTestId('field-secret').fill('s3cr3t!');
    await pageA.getByTestId('field-notes').fill('shared account');
    await pageA.getByTestId('action-add_entry').click();

    // Every other member (node B) sees the new entry in the vault list.
    await expect(
      pageB.locator('[data-testid^="item-vaultentry-"]').filter({ hasText: serviceName }),
    ).toBeVisible({ timeout: 5_000 });

    await clearAuth(pageA);
    await clearAuth(pageB);
    await ctxA.close();
    await ctxB.close();
  });
});
