# Workshop Apps

Calimero apps built during workshops using [Calimero Studio](https://calimero.network).

Each app lives on its own branch:
`workshop/{participant}/{app-name}-{timestamp}`

## Browse apps

See the [branches](../../branches) tab for all workshop builds.
